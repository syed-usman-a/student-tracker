//
This program allows users to input 3 files that include one file for student information, one file for course information, and one file that links students to their respective courses,
the program then allows the user to generate a new text file called report.txt which includes a report of all students and the classes they are registered in,
then the user is able to request to output the student with the highest average grade, manually search for a student via their student ID and modify student attributes.

File Format Instructions (Sample Files Are Included):
(IMPORTANT ALL DATA FEILDS MUST BE SEPERATED BY A COMMA)

    students.txt:
    Student id followed by their first name followed by their last name then lastly their date of birth. 
    Student ID, First Name, Last Name, Date of Birth
    (int, string, string ,string)
    example:
    (40300001, Syed Usman, Ali, 27/11/2006)

    courses.txt:
    Course code followed by the course title followed by the number of credits awarded for completing the course.
    Course Code, Course Title, Number of Credits
    (string, string, int)
    example:
    (COEN243, Programming Methodology I, 3)

    registrations.txt:
    Student id followed by the course code followed by their final grade for that course.
    Student ID, Course Code, Final Grade
    (int, string, float)
    example:
    (40300001, COEN243, 100.0)

executable is already provided, if you wish to create one your self here are the instructions below: 
Compilation Instructions:
    Make sure that you have a c++ compiler
    Make sure that all files (class header files, class implementation cpp files and main cpp program) required for compilation are all located in one directory (file).
    Make sure that all the files can be compiled at once:
    
        For direct compilation in VS code editor.
        tasks.json file should look like this, leave everything as it is, change only the highlighted part
        This is to make all files are compiled and linked correctly:
            {
            "tasks": [
                {
                    "type": "cppbuild",
                    "label": "C/C++: g++.exe build active file",
                    "command": "C:\\ProgramData\\mingw64\\mingw64\\bin\\g++.exe",

-------------------------------------------------------------------------------------------------------------------------------------------------------
                    "args": [
                        "-fdiagnostics-color=always",
                        "-g",
                        "${workspaceFolder}\\*.cpp", ---- IMPORTANT
                        "-o",
                        "${fileDirname}\\Project.exe" ---- You can choose the name of the executable by modifying the name here (Project.exe)
                    ],
-------------------------------------------------------------------------------------------------------------------------------------------------------

                    "options": {
                        "cwd": "${fileDirname}"
                    },
                    "problemMatcher": [
                        "$gcc"
                    ],
                    "group": {
                        "kind": "build",
                        "isDefault": true
                    },
                    "detail": "Task generated by Debugger."
                }
            ],
            "version": "2.0.0"
        }

        For compilation using cmd:
            navigate to the directory using cd file location eg. (cd "C:\Users\Surface\Desktop\Project")
            then use this to compile the project (*.cpp -o Project.exe) (Creates an executable called Project.exe)
            if everything compiles successful you should be able to see the exe file in the chosen directory




